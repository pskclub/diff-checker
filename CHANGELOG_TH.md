# บันทึกการเปลี่ยนแปลง - การปรับปรุง PDF Support

## 📅 วันที่: 2025-11-10

## 🎯 เป้าหมาย
ปรับปรุงการรองรับไฟล์ PDF ให้ดีขึ้นและรักษา layout ของข้อความได้สมบูรณ์มากขึ้น

---

## ✨ การปรับปรุงหลัก

### 1. 🔄 เปลี่ยน PDF Library
- **ลบ**: `pdf-parse` (มีข้อจำกัดในการควบคุม text extraction)
- **เพิ่ม**: `pdfjs-dist` (library จาก Mozilla Firefox ที่มีประสิทธิภาพสูง)
- **เพิ่ม**: `@types/pdfjs-dist` สำหรับ TypeScript support

### 2. 📐 การรักษา Layout และ Spacing

#### ✅ ฟีเจอร์ใหม่:
- **รักษา Indentation**: ตรวจจับและรักษาการเยื้องของย่อหน้า
- **คำนวณ Spacing**: ใช้ระยะห่างจริงระหว่างคำเพื่อเพิ่ม spaces ที่เหมาะสม
- **รักษา Line Breaks**: ตรวจจับการขึ้นบรรทัดใหม่อย่างแม่นยำ
- **จัดการ Whitespace**: ลบ whitespace ที่ไม่จำเป็นแต่รักษาที่สำคัญ

#### 🎨 การทำงาน:
```
เดิม:
HelloWorldThisisatest

ใหม่:
Hello World    This is a test
```

### 3. 📰 รองรับ Multi-Column Layouts

#### ✅ ฟีเจอร์:
- **ตรวจจับ Columns อัตโนมัติ**: วิเคราะห์ตำแหน่ง X เพื่อหา column breaks
- **อ่านตาม Column Order**: อ่านจากซ้ายไปขวา แล้วไปยัง column ถัดไป
- **Column Separators**: เพิ่มบรรทัดว่างระหว่าง columns

#### 🎨 ตัวอย่าง:
```
Column 1 Text    |    Column 2 Text
More text here   |    More text there

กลายเป็น:

Column 1 Text
More text here

Column 2 Text
More text there
```

### 4. 📄 Page Management

#### ✅ ฟีเจอร์:
- **Page Separators**: เพิ่ม "--- Page Break ---" ระหว่างหน้า
- **Blank Line Control**: จำกัด consecutive blank lines ไม่เกิน 2 บรรทัด
- **Page Numbering**: รักษาข้อมูลหน้าไว้

### 5. 🔧 Technical Improvements

#### ✅ การแก้ไข:
- **Dynamic Import**: ใช้ dynamic import เพื่อหลีกเลี่ยงปัญหา SSR
- **Worker Configuration**: ตั้งค่า PDF.js worker อย่างถูกต้อง
- **Coordinate Conversion**: แปลง PDF coordinates จาก bottom-up เป็น top-down
- **Type Safety**: เพิ่ม type checking ที่ดีขึ้น

---

## 📊 การเปรียบเทียบ

### เดิม (pdf-parse):
```typescript
const parser = new PDFParse({ data: arrayBuffer });
const res = await parser.getText();
return res.pages.map((page) => page.text).join('\n');
```

**ปัญหา:**
- ไม่รักษา spacing
- ไม่รักษา indentation
- ไม่รองรับ multi-column
- ควบคุม layout ไม่ได้

### ใหม่ (pdfjs-dist):
```typescript
const pdfjsLib = await import('pdfjs-dist');
const pdf = await pdfjsLib.getDocument({ data: arrayBuffer }).promise;

// วิเคราะห์ text items แต่ละตัว
// คำนวณ positions และ spacing
// ตรวจจับ columns
// สร้าง lines ด้วย proper formatting
```

**ข้อดี:**
- ✅ รักษา spacing และ indentation
- ✅ รองรับ multi-column layouts
- ✅ ควบคุม text extraction ได้ละเอียด
- ✅ รักษา layout structure

---

## 🔍 Algorithm Details

### 1. Text Extraction Process

```
1. Load PDF Document
   ↓
2. For each page:
   ├─ Get text content
   ├─ Get viewport dimensions
   └─ Extract text items with positions
   ↓
3. Analyze Layout:
   ├─ Detect columns (X position analysis)
   ├─ Group items by column
   └─ Sort by column → Y → X
   ↓
4. Build Lines:
   ├─ Calculate indentation
   ├─ Add spacing between words
   └─ Detect line breaks
   ↓
5. Post-Process:
   ├─ Remove excessive blank lines
   └─ Add page separators
```

### 2. Key Parameters

```typescript
// Column Detection
const columnThreshold = 100;  // Gap ขั้นต่ำสำหรับ column break

// Line Detection
const sameLineThreshold = 3;  // Y difference สำหรับบรรทัดเดียวกัน

// Spacing
const minGapForSpace = 5;     // Gap ขั้นต่ำเพื่อเพิ่ม space
const maxSpaces = 10;         // Spaces สูงสุดที่เพิ่มได้

// Formatting
const maxBlankLines = 2;      // Blank lines ติดกันสูงสุด
```

---

## 📦 Dependencies Changes

### เพิ่ม:
```json
{
  "dependencies": {
    "pdfjs-dist": "^5.4.296"
  },
  "devDependencies": {
    "@types/pdfjs-dist": "^2.10.378"
  }
}
```

### ลบ:
```json
{
  "dependencies": {
    "pdf-parse": "^2.4.5"  // ❌ ถูกลบออก
  }
}
```

---

## 🧪 การทดสอบ

### ทดสอบกับ PDF ประเภทต่างๆ:

1. **✅ Simple Text PDF**
   - ข้อความธรรมดา 1 column
   - ผลลัพธ์: ดีมาก

2. **✅ Multi-Column PDF**
   - เอกสารที่มี 2-3 columns
   - ผลลัพธ์: รักษา column order ได้ดี

3. **✅ PDF with Tables**
   - เอกสารที่มี tables
   - ผลลัพธ์: รักษา alignment ได้ในระดับหนึ่ง

4. **✅ PDF with Indentation**
   - เอกสารที่มีการเยื้อง
   - ผลลัพธ์: รักษา indentation ได้ดี

5. **✅ Mixed Layout PDF**
   - เอกสารที่มี layout ผสม
   - ผลลัพธ์: จัดการได้ดี

---

## ⚠️ ข้อจำกัด

### ยังไม่รองรับ:
1. **Scanned PDFs**: ต้องใช้ OCR (อาจเพิ่มในอนาคต)
2. **Rotated Text**: ข้อความที่หมุนอาจไม่ถูกต้อง
3. **Complex Tables**: Tables ที่ซับซ้อนมากอาจไม่สมบูรณ์
4. **Images**: ไม่ extract text จากรูปภาพ

### ข้อควรระวัง:
1. **ไฟล์ขนาดใหญ่**: PDF หลายร้อยหน้าอาจใช้เวลานาน
2. **Memory Usage**: ใช้ memory มากกับไฟล์ใหญ่
3. **Font Encoding**: บาง fonts อาจมีปัญหา

---

## 🚀 การใช้งาน

### วิธีใช้:
1. เปิดแอปพลิเคชัน
2. เลือกไฟล์ PDF ที่ต้องการเปรียบเทียบ
3. ระบบจะ extract text โดยอัตโนมัติ
4. ดูผลการเปรียบเทียบที่รักษา layout ได้ดี

### ตัวอย่างผลลัพธ์:

**ไฟล์ PDF ต้นฉบับ:**
```
    Chapter 1: Introduction
    
This is the first paragraph with proper indentation.
This continues on the same line.

    Section 1.1: Overview
    
More content here...
```

**Text ที่ Extract ได้:**
```
    Chapter 1: Introduction
    
This is the first paragraph with proper indentation.
This continues on the same line.

    Section 1.1: Overview
    
More content here...
```

---

## 🎯 ผลลัพธ์

### ก่อนปรับปรุง:
- ❌ Layout พัง
- ❌ Spacing หาย
- ❌ Indentation หาย
- ❌ Multi-column อ่านผิด

### หลังปรับปรุง:
- ✅ รักษา layout ได้ดี
- ✅ Spacing ถูกต้อง
- ✅ Indentation สมบูรณ์
- ✅ Multi-column อ่านถูกต้อง

---

## 📈 การพัฒนาต่อในอนาคต

### แผนการพัฒนา:
1. **OCR Support**: รองรับ scanned PDFs
2. **Table Detection**: ตรวจจับ tables ได้ดีขึ้น
3. **Font Analysis**: วิเคราะห์ font และ style
4. **Progress Indicator**: แสดง progress สำหรับไฟล์ใหญ่
5. **Web Worker**: ย้าย processing ไป worker
6. **Caching**: cache ผลลัพธ์

---

## 📝 สรุป

การปรับปรุงครั้งนี้ทำให้ **Text Diff Checker** รองรับไฟล์ PDF ได้ดีขึ้นอย่างมาก:

✅ **รักษา Layout**: spacing, indentation, line breaks
✅ **Multi-Column**: รองรับเอกสารหลาย columns
✅ **ประสิทธิภาพ**: ใช้ pdfjs-dist ที่เร็วและแม่นยำ
✅ **SSR Compatible**: ทำงานได้ดีกับ Next.js
✅ **Type Safe**: มี TypeScript types ครบถ้วน

**ผลลัพธ์**: การเปรียบเทียบไฟล์ PDF ที่แม่นยำและอ่านง่ายมากขึ้น! 🎉

